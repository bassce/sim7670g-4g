# Waveshare SIM7670G V2 适配

本地适配目标：**ESP32-S3-A-SIM7670X-4G-V2，实际装配 SIM7670G**。编译环境为 `WS-SIM7670G-V2_BLE`，也是本目录默认环境。不能用来代替 V1 或 A7670E 固件。

源码基于 adlerre/obd2-mqtt 提交 `54a950a6bea7600c9faae508bf1f23c8cfc0748e`。新环境使用 Espressif32 6.11.0、Arduino ESP32 2.0.17，并将 Lewis He TinyGSM 固定到 `2f9c30559543e3c360604799432c10bb1285ca0f`。驱动选择为 `TINY_GSM_MODEM_SIM7670G`，内部对应 SIM7672 驱动，不定义 A7670/A76XXSSL 宏。现有 A7670E 环境仍可单独选择。

## 电路对应

依据用户提供的 `ESP32-S3-A-SIM7670X-4G-V2.pdf`，GPIO 方向以 ESP32 为准。

| 功能 | V2 配置 |
|---|---|
| MCU | ESP32-S3R8 |
| Flash / PSRAM | 16MB Quad Flash / 8MB Octal PSRAM，`qio_opi` |
| 主 AT 串口 | 115200，8N1；ESP TX=18、RX=17 |
| 模组供电 | GPIO21，高电平打开 VBAT→VVBAT |
| PWRKEY | Q9 由 VVBAT 驱动，没有 ESP GPIO，不执行 GPIO4 脉冲 |
| RESET | 没有接 ESP GPIO，不驱动 GPIO5 |
| DTR / RI | GPIO45 置低保持唤醒 / GPIO40 输入 |
| MAX17048 | 地址 0x36，SDA=15、SCL=16 |
| USB 下载和日志 | CH343 / UART0；USB CDC on boot 关闭 |
| GNSS | 通过模组主 AT 串口控制内部 GNSS，无外部 ESP GPS UART |

GPIO33–37 留给 Octal PSRAM。相机、TF 卡、RGB 灯和 USB PPP 没有接入应用。MAX17048 显示板上电源域/18650 的电量，不是车辆动力电池 SOC。

## 行为变化

- 模组启动等待 AT 最多约 20 秒；初始化、注册、PDP 建连采用有限次调用，失败后返回。网络失败间隔 30 秒重试，健康状态每 5 秒检查一次。单次底层 AT 操作仍会等待驱动规定的超时，不是完全异步。
- 检查注册状态和 PDP 数据连接，处理“仍注册但数据连接已断开”。持续失败后尝试正常关机及 GPIO21 断电恢复；没有无限 PWRKEY 循环，也不因初始化失败不断重启 ESP32。
- 断网和模组重启时清理旧 TCP/TLS 客户端状态；MQTT 地址、鉴权失败会等待后再重试。
- `AT+IFC=0,0` 禁用未连接的 RTS/CTS，`AT+CSCLK=0` 禁用模组睡眠。
- GNSS 使用 `CGNSSPWR=1` / `CGNSSINFO`，等待 `OK`，允许手册规定的 10 秒响应。未定位或异常坐标不发布；DOP 不再冒充米制 `gps_accuracy`。
- MAX17048 检查 I2C 传输及返回长度，读取失败时跳过该次电量发布。
- 新板型加入 CI、发布环境列表与 Web Installer 清单生成器；其芯片类型为 ESP32-S3，bootloader 地址为 0。
- 构建版本信息改为跨平台 Python 脚本，支持 Windows 和无 `.git` 的源码压缩包。

## 构建与上传

安装 Python、PlatformIO、Node.js 24 和 npm 后，在源码目录运行：

```text
pio run -e WS-SIM7670G-V2_BLE
pio run -e WS-SIM7670G-V2_BLE -t buildfs
```

构建产物在 `.pio/build/WS-SIM7670G-V2_BLE/`。首次安装需要固件及 LittleFS 网页。下列命令会实际写入开发板，应在确认串口后自行执行：

```text
pio run -e WS-SIM7670G-V2_BLE -t upload --upload-port COMx
pio run -e WS-SIM7670G-V2_BLE -t uploadfs --upload-port COMx
pio device monitor --port COMx --baud 115200
```

`COMx` 换成实际串口。根据板上标记将 USB 路由切到 ESP32 下载路径；如不能自动进入下载模式，按住 BOOT 再按 RESET。复位按键用于 ESP32 下载操作，与不存在的“模组 RESET GPIO”是不同的连接。

使用底层刷写工具时，地址对应如下，尤其不要沿用普通 ESP32 的 bootloader 0x1000：

| 文件 | 写入地址 |
|---|---|
| bootloader.bin | 0x0000 |
| partitions.bin | 0x8000 |
| boot_app0.bin | 0xE000 |
| firmware.bin | 0x10000 |
| littlefs.bin | 0x790000 |

沿用上游 `partition-custom-ota-16m.csv`：两个应用槽各 0x3C0000 字节，LittleFS 为 0x60000 字节，分区占用前 8MB，其余 Flash 暂未分配。`uploadfs` 会覆盖文件系统中的已有配置；升级前导出需要保留的设置。

启动日志应显示 `Flash=16 MB, PSRAM=8 MB, TX=18 RX=17 POWER=21`。连接 `OBD2-MQTT-...` Wi-Fi，在 `http://192.168.4.1` 设置运营商 APN、MQTT 和 BLE OBD 适配器，保存并重启。ESP32-S3 只支持 BLE，不能连接仅支持经典蓝牙 SPP 的 ELM327。原项目在 Wi-Fi AP 被使用时暂停输出任务，完成设置后断开配置 Wi-Fi 再观察 LTE/MQTT。

## GNSS 坐标格式

SIM767XX AT 手册 V1.01 第 341 页的字段定义写十进制度，而同页示例是度分格式。本适配默认 `WS_GNSS_COORDINATE_FORMAT=2`：数值超出纬度 90 / 经度 180 时按度分解析，否则按十进制度解析。这能处理两种常见响应，但**靠近赤道和本初子午线的度分值可能与十进制度歧义**。

首次户外定位应对照实际位置及 AT 日志。确认模组固件格式后，可在新环境 `build_flags` 添加：

```text
-DWS_GNSS_COORDINATE_FORMAT=0
```

`0` 固定十进制度，`1` 固定度分，`2` 自动。不要把另一份 A7670E 的解析格式直接套用。需要 AT 日志时可以临时添加 `-DDUMP_AT_COMMANDS`；日志可能含 APN、服务器或连接资料，分享前检查内容。

## 验证范围与实机项目

本地构建结果及校验和另见交付目录的验证记录。源码修改没有通过 USB 向开发板写入，也没有进行上车测试。

需在实机验证冷启动、ESP32 单独复位、反复断电、SIM/PIN/APN、LTE 掉线恢复、BLE+MQTT 同时运行、户外 GNSS、MAX17048 和长时间稳定性。BAT_SET 拨码也连接供电控制节点；软件断电测试必须确认拨码未强制供电，并实际测量 VVBAT 能下降。Q9 持续拉低 PWRKEY 的启动/关机行为还需按实际模组固件验证。

TCP MQTT 沿用 PubSubClient。TLS 使用固定版本 TinyGSM 的 CCH 通道实现，尚未做真实 broker、分段接收、重连和证书验证测试；上游在未配置 CA 时默认 `authmode=0`，因此现有“Secure”选项不能当作已验证服务器身份。TLS 应视作实验功能，不能仅凭编译成功认定可投入无人值守使用。

当前没有实现停车整板低功耗，MAX17048 分支仍为 `DEVICE_CAN_DEEP_SLEEP=false`；基站定位没有启用。这次也未修改 ID.4/MEB PID 数据配置或车辆诊断算法。

## 本地解析测试

用宿主 C++ 编译器运行实际 GNSS 解析器，覆盖 25 个有效/无效响应：

```text
c++ -std=c++11 -Isrc test/host/gnss_test.cpp -o gnss_test
./gnss_test
```

在已经单独完成 `npm run build` 的情况下，可设置环境变量 `OBD2_MQTT_PREBUILT_UI=1` 再运行 `pio ... -t buildfs`，复用网页并重新写入设备型号；缺少 `index.html` 时构建会失败。

## 参考

- [obd2-mqtt 源码基线](https://github.com/adlerre/obd2-mqtt/tree/54a950a6bea7600c9faae508bf1f23c8cfc0748e)
- [固定版本 TinyGSM SIM7672 驱动](https://github.com/lewisxhe/TinyGSM/blob/2f9c30559543e3c360604799432c10bb1285ca0f/src/TinyGsmClientSIM7672.h)
- [Waveshare ESP32-S3-SIM7670G-4G 官方资料](https://docs.waveshare.net/ESP32-S3-SIM7670G-4G/)
- 用户提供的 V2 原理图、SIM767XX AT 手册 V1.01、SIM7672X 硬件设计 V1.01 和 SSL 应用说明 V1.00。
