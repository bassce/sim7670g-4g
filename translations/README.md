# 外挂语言包

服务器与 Gitea 镜像使用相同目录。`en.json`、`zh-CN.json` 是格式起始样例（`status: seed`），不是完整界面翻译；当前固件尚未接入在线加载。

使用固定文案键和 UTF-8 JSON，不附带字体。后续按需下载并缓存当前语言，英文兜底；校验大小、版本和格式后再替换旧包。日语、韩语、法语可按相同格式增加。用户自定义配置键、MQTT 标识和 PID `name` 不翻译。
